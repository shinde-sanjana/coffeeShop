import 'package:flutter/material.dart';

class Coffee {
  String name, image;
  Color backgroundColor;
  double price;
  Coffee({required this.backgroundColor, required this.image,required this.name,required this.price});
}
